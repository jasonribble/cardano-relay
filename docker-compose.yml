version: "3.9"

services:
  cardano-relay:
    container_name: cardano-relay
    build:
      dockerfile: ./Dockerfile.relay 
    network_mode: "host"
    restart: always
    volumes:
      - ./node-db:/data/db
      - ./node-config:/config
      - ./node-ipc:/ipc
    logging:
      driver: "json-file"
      options:
        max-size: "200k"
        max-file: "10"
  cardano-producer:
    container_name: cardano-producer
    build:
      dockerfile: ./Dockerfile.relay 
    network_mode: "host"
    restart: always
    volumes:
      - ./node-db:/data/db
      - ./node-config:/config
      - ./node-ipc:/ipc
    logging:
      driver: "json-file"
      options:
        max-size: "200k"
        max-file: "10"


volumes:
  node-db:
  node-ipc:
